import React from 'react';
import { Button } from 'src/components';

export const ButtonPage: React.FC = () => {
  return (
    <div style={{ margin: 'auto' }}>
      <Button>ButtonButtonButtonButtonButton</Button>
    </div>
  );
};

export default ButtonPage;
